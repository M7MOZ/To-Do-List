import React from "react";
import ReactDOM from "react-dom";
import './index.css'; // You may or may not have this depending on your project setup
import App from "./App"; // This is the main component of your application

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
